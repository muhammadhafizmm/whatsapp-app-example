{
  "name": "whatsapp-webhook-example",
  "version": "0.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "sst dev",
    "build": "tsc --build",
    "test": "vitest run --config vitest.config.ts --coverage",
    "typecheck": "tsc --noEmit",
    "deploy": "sst deploy",
    "remove": "sst remove",
    "lint": "echo 'Add linter (e.g. eslint) if needed'",
    "clean": "rm -rf .sst .build node_modules && npm install"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22",
    "@vitest/coverage-v8": "^3.2.4",
    "typescript": "^5",
    "vitest": "^3.2.4"
  },
  "dependencies": {
    "@fastify/aws-lambda": "^5.1.4",
    "@fastify/multipart": "^9.0.3",
    "@types/busboy": "^1.5.4",
    "fast-glob": "^3.3.3",
    "fastify": "^5.4.0",
    "sst": "^3"
  }
}
